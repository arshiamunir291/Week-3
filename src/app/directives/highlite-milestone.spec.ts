import { HighliteMilestone } from './highlite-milestone';

describe('HighliteMilestone', () => {
  it('should create an instance', () => {
    const directive = new HighliteMilestone();
    expect(directive).toBeTruthy();
  });
});
